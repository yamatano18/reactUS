const SERVER= "localhost";
const PORT = 9090;
const HTTP_SERVER_PORT = 'http://' + SERVER + ':' + PORT;
const IMAGES = "images";
const HTTP_SERVER_PORT_IMAGES = HTTP_SERVER_PORT + '/' + IMAGES;

export {SERVER, PORT, HTTP_SERVER_PORT, HTTP_SERVER_PORT_IMAGES, IMAGES};